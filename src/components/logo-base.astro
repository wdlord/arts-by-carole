---
import { Image } from "astro:assets";
import { siteName } from "../data/constants";

interface Props {
  src: ImageMetadata;
  height?: number;
}

const {src, height = 40 } = Astro.props;
---

<div>
  <Image
    src={src}
    alt={`${siteName} logo`}
    class="logo"
    draggable="false"
  />
</div>

<style
  define:vars={{"logo-height": `${height}px` }}
>
  .logo {
    width: auto;
    height: var(--logo-height);
  }
</style>
